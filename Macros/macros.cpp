#include <bits/stdc++.h>
using namespace std;

#define PI 3.14 // macro

int main()
{

    int r = 5;
    double area = PI * r * r;
    cout << area << endl;

    return 0;
}

// a piece of code in a program which is replaced by the value of a macro